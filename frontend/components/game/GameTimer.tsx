'use client';

/**
 * @file GameTimer — Timer is intentionally disabled in relaxed mode.
 * @description This component renders nothing to avoid countdown pressure.
 */

export default function GameTimer() {
  return null;
}

